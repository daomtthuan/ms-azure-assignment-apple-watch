@helper NavbarLinkItem(string title, string url, bool active) {
  if (active) {
    <a class="nav-link active" aria-current="page" href="@url">@title</a>
  } else {
    <a class="nav-link" href="@url">@title</a>
  }
}
